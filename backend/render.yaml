services:
  - type: web
    name: parliament-chat-backend
    env: python
    buildCommand: pip install fastapi uvicorn cryptography python-multipart websockets bcrypt "python-jose[cryptography]"
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT